<%= form_for [@post_activity, @comment] do |f| %>

	<%= f.label :content %>
	<%= f.text_area :content, :rows => '10', class:'form-control'%>
	<br>
	<%= f.submit "submit", class:'btn btn-primary' %>

<% end %>
